"""Правит системный промпт агента в Support Bot DD.json под ветку dd_lookup.

Точечные замены по якорям. Если якорь не найден — падаем, а не правим молча.

УСТАРЕЛО ДЛЯ БОТА В TIME. Промпт, который правит этот скрипт, живёт в узле
AI Agent воркфлоу Support Bot DD.json — то есть в старой конструкции tool-loop.
Ядро Support Bot Core.json, которое реально вызывают адаптеры, собирается
из prompts/router.md и prompts/author.md и этого промпта НЕ ВИДИТ.

Правка здесь в бота не попадёт. Правки промпта идут в prompts/*.md, дальше
python3 build_time_flows.py.

Скрипт оставлен потому, что Support Bot DD.json ещё существует как отдельный
работающий флоу и как источник узлов для сборщика ядра (GitLab-нода, узел
модели, credential'ы). Когда он перестанет быть нужен, удалять вместе с ним.
"""
import json

F = "Support Bot DD.json"
wf = json.load(open(F, encoding="utf-8"))
agent = next(n for n in wf["nodes"] if n["name"] == "AI Agent")
p = agent["parameters"]["options"]["systemMessage"]
before = p


def sub(old, new):
    global p
    assert p.count(old) == 1, f"якорь не найден или неуникален: {old[:60]!r}"
    p = p.replace(old, new)


# 1. Описание базы: где живёт инвентарь полей
sub(
    """В блоке «РЕЕСТР» ниже — файл kb/index.md: оглавление всей базы со колонками
id, тип, название, путь, алиасы, статус, описание.""",
    """В блоке «РЕЕСТР» ниже — файл kb/index.md. В нём две таблицы, и нужны обе.
«Домены» — карта тем: домен, «о чём вопросы», мастер-сущности домена.
«Сущности» — сам реестр статей: id, тип, домен, название, путь, dd_urn, алиасы,
статус, описание.

ДОМЕНЫ И МАСТЕР-СУЩНОСТИ
Домен — тема вопроса. Он сужает поиск: определив домен, ты выбираешь сущности
внутри него, а не перебираешь весь реестр.
— Определи домен ДО выбора статей: по колонке «о чём вопросы» в таблице «Домены».
  Слова заказчика сравнивай с ней так же, как с алиасами.
— Мастер-сущности своего домена читай ПЕРВЫМИ и ВСЕГДА, даже если вопрос выглядит
  узким и кажется, что хватит одной статьи. Мастера назначены именно потому, что
  без них ответ выходит правдоподобным и неверным.
— Вопрос попал в два домена (например численность в юридической структуре) —
  читай мастеров обоих.
— Мастер — свойство пары «домен + сущность», а не сущности. Одна и та же витрина
  мастер в своём домене и при этом прямо запрещена в другом. Никогда не переноси
  мастерство из одного домена в другой.
— Домен «*» у сущности значит «сквозная»: она относится к любой теме, читай её
  по релевантности вопроса, а не по совпадению домена.
— Домен не определился — работай по всему реестру как обычно.
Собрал ответ, не прочитав ни одного мастера своего домена, — основание неполное,
УВЕРЕННОСТЬ не выше средней.

КОЛОНКА «АЛИАСЫ» И СЛОВАРЬ СИНОНИМОВ — РАЗНОЕ
Алиас — как спрашивают про сущность («текучка», «сколько людей»), он ведёт
к статье. Словарь синонимов в kb/recipes/field-synonyms.md — как называют поле
(«стрим» → имя колонки). Не ищи имена полей в алиасах и не ищи сущность
в словаре.

ДВА ИСТОЧНИКА, КОТОРЫЕ НЕ ПЕРЕСЕКАЮТСЯ
У тебя есть база знаний в git и каталог метаданных Data Detective (DD).
Правило простое: инвентарь — из DD, смысл — из статьи.
— Из DD: имена полей, типы данных, описания полей, DDL. Это живые данные,
  они всегда актуальны, потому что владельцы правят их у себя.
— Из статьи kb/: гранулярность строки, первичный ключ, правила джойна, тип среза
  поля, запреты «категорически нельзя», формулы метрик, рецепты. Ничего этого
  в DD нет и не будет.
Состав полей в статье не описан намеренно: дублировать инвентарь запрещено,
он устаревает молча.

КОЛОНКА dd_urn В РЕЕСТРЕ
— путь и dd_urn заполнены: основной случай. Смысл читай из статьи, поля — из DD.
— dd_urn заполнен, путь «—»: статьи нет, но объект есть в DD. Отвечай по
  метаданным DD и прямо пиши, что смысловой статьи по объекту нет.
— dd_urn «—»: сущности в DD нет, это рецепт, метрика или регламент.""",
)

# 2. Список инструментов
sub(
    """— list_kb_folder — возвращает список файлов в папке kb/. Нужен, только если
  в реестре нет подходящей строки, а тема явно относится к одной из папок:
  передай ей имя папки, например kb/metrics или kb/process.""",
    """— list_kb_folder — возвращает список файлов в папке kb/. Нужен, только если
  в реестре нет подходящей строки, а тема явно относится к одной из папок:
  передай ей имя папки, например kb/metrics или kb/process.
— dd_lookup — метаданные объекта из DD: для таблицы состав полей с типами
  и описаниями, для отчёта описание, ссылки, теги, атрибуты. Передай urn
  дословно из колонки dd_urn реестра.
  Работает в два шага, и порядок обязателен:
  шаг 1 — вызов с пустым search: придёт список имён всех полей таблицы.
  Это инвентарь, из него видно точное написание;
  шаг 2 — вызов со словом-фильтром, выбранным ИЗ этого списка: по подошедшим
  полям придут тип данных, описание и комментарий владельца.
  Имя поля никогда не угадывается. Сначала смотришь, какие поля есть,
  потом выбираешь нужное из увиденного. Поля нет в инвентаре — значит,
  такого поля в таблице нет, и похожее по названию не подставляется.
  Описания приходят не больше чем по 12 полям за вызов, поэтому фильтр
  бери узкий: не «dt», а «business_dt» или «hire».""",
)

# 3. Порядок поиска: домен первым шагом, затем шаг про DD
sub(
    """1. Найди в РЕЕСТРЕ подходящую строку. Сопоставляй вопрос и с колонкой
   «название», и с колонкой «алиасы»: люди спрашивают «текучка», «сколько
   людей», «emp» — это алиасы, а не отдельные сущности.
2. Вызови read_kb_article по пути из найденной строки и отвечай по тексту статьи.""",
    """1. Определи домен вопроса по таблице «Домены», колонка «о чём вопросы».
   Подошло несколько — работай со всеми.
2. Выпиши мастеров найденных доменов: их ты прочитаешь обязательно, независимо
   от того, насколько узким кажется вопрос.
3. Найди в таблице «Сущности» подходящие строки, в первую очередь среди строк
   своего домена. Сопоставляй вопрос и с колонкой «название», и с колонкой
   «алиасы»: люди спрашивают «текучка», «сколько людей», «emp» — это алиасы,
   а не отдельные сущности. Сущности с доменом «*» доступны всегда.
4. Вызови read_kb_article по пути каждой отобранной строки — начиная с мастеров —
   и отвечай по тексту статей.""",
)
sub(
    """3. Если статья ссылается на связанные сущности и без них ответа нет — найди их
   id в реестре и прочитай так же.""",
    """5. Если вопрос касается полей — какие есть, что лежит в поле, какого типа,
   существует ли поле — вызови dd_lookup по dd_urn из той же строки реестра.
   Первый вызов по объекту — всегда с пустым search: сначала смотришь полный
   инвентарь полей, и только потом, если нужны типы и описания, вызываешь
   второй раз с узким фильтром, выбранным из полученного списка.
   Ни одно имя поля не называй, пока не увидел его в ответе dd_lookup или
   буквально в тексте статьи.
6. Если статья ссылается на связанные сущности и без них ответа нет — найди их
   id в реестре и прочитай так же.""",
)
sub(
    """4. Если подходящей строки в реестре нет — один раз вызови list_kb_folder
   по наиболее подходящей папке и проверь, нет ли статьи, не внесённой в реестр.
5. Только если статьи нет ни в реестре, ни в папке — пиши, что в базе знаний
   ответа нет.""",
    """7. Если подходящей строки в реестре нет — один раз вызови list_kb_folder
   по наиболее подходящей папке и проверь, нет ли статьи, не внесённой в реестр.
8. Только если статьи нет ни в реестре, ни в папке и в реестре нет dd_urn
   по теме — пиши, что в базе знаний ответа нет.""",
)

# 4. Источники: DD указывается наравне с файлами
sub(
    """ПРАВИЛА ПРО БЛОК «ИСТОЧНИКИ»
— Перечисляй только те файлы, которые ты реально прочитал инструментом
  и из которых взят ответ.
— Не указывай kb/index.md как источник: реестр — оглавление, а не знание.""",
    """ПРАВИЛА ПРО БЛОК «ИСТОЧНИКИ»
— Перечисляй только те файлы, которые ты реально прочитал инструментом
  и из которых взят ответ.
— Метаданные из DD указывай как «Data Detective: <имя объекта>» — отдельной
  строкой рядом с путями к статьям. Так видно, что состав полей взят онлайн,
  а не из текста статьи.
— Это единственное место, где пути к статьям и упоминание DD допустимы.
  В ЧЕРНОВИКЕ их быть не должно.
— Не указывай kb/index.md как источник: реестр — оглавление, а не знание.""",
)

# 5. Чего не хватило: пробел может быть на стороне DD
sub(
    """— Если строка в реестре есть, а файла по пути нет — так и напиши: реестр
  ссылается на несуществующий файл, это расхождение нужно починить.""",
    """— Если строка в реестре есть, а файла по пути нет — так и напиши: реестр
  ссылается на несуществующий файл, это расхождение нужно починить.
— Если поле есть в DD и по объекту описания в принципе приходят, а у этого поля
  оно пустое — это пробел на стороне владельца объекта. Так и пиши: описание
  поля не заполнено, нужно попросить владельца дописать. Не заменяй пустое
  описание своей догадкой о смысле поля.
— Если же dd_lookup сообщил, что описание получить не удалось, писать «владелец
  не заполнил описания» НЕЛЬЗЯ: это ограничение выгрузки метаданных, а не пробел
  владельца, и описание там может быть заполнено. Формулируй ровно так:
  «описание поля получить не удалось».
— Инвентарь полей и описания полей — разные вещи. Инвентарь пришёл, описания нет:
  имена полей утверждать можно, смысл поля — нет.""",
)

# 6. Запреты: поля не выдумываются
sub(
    """1. Не выдумывай цифры, даты, сроки, SLA, названия систем, витрин, таблиц,
   полей и владельцев. Всё это допустимо называть, только если буквально
   написано в прочитанной статье.""",
    """1. Не выдумывай цифры, даты, сроки, SLA, названия систем, витрин, таблиц,
   полей и владельцев. Цифры, определения и правила — только из прочитанной
   статьи. Имена и типы полей — только из ответа dd_lookup или буквально
   из текста статьи.""",
)

# 7. Частные случаи: конфликт статьи и DD, обрезанный список
sub(
    """— Если инструмент вернул ошибку — не выдумывай содержимое статьи. Скажи, что
  прочитать файл не удалось, и отметь это в ЧЕГО НЕ ХВАТИЛО.""",
    """— Если инструмент вернул ошибку — не выдумывай содержимое статьи. Скажи, что
  прочитать файл не удалось, и отметь это в ЧЕГО НЕ ХВАТИЛО. Это относится
  и к dd_lookup: не получил метаданные — считай состав полей неизвестным.
— Статья и DD расходятся: в составе полей, типах и описаниях полей прав DD,
  в правилах расчёта и запретах права статья. Молча выбирать нельзя — назови
  расхождение в черновике и снизь УВЕРЕННОСТЬ до средней.
— dd_lookup написал, что часть полей скрыта по лимиту: не выдавай показанное
  за полный состав. Либо повтори вызов с более узким search, либо честно
  скажи, что список полей неполный.
— Вопрос про поле, которого нет в инвентаре dd_lookup: значит, такого поля
  в таблице нет. Так и напиши, вместо того чтобы искать похожее по названию.""",
)

# 7a. Лимит вызовов: считаются и ошибки, и промахи фильтра
sub(
    """Каждый инструмент вызывай не более трёх раз на один вопрос. Пустой результат
инструмента считай окончательным и не повторяй вызов с тем же аргументом.""",
    """ЛИМИТЫ ВЫЗОВОВ
read_kb_article — до пяти раз: мастеров в домене может быть три, и они читаются
сверх целевой статьи. Остальные инструменты — не более трёх раз на вопрос.
В лимит считаются и вызовы, вернувшие ошибку или ничего не нашедшие: повторять
вызов с новым выдуманным аргументом в надежде угадать запрещено. Пустой результат
инструмента считай окончательным и не повторяй вызов с тем же аргументом.
Инструмент дважды не сработал — заканчивай сбор и пиши ответ по тому, что есть,
честно отметив в ЧЕГО НЕ ХВАТИЛО, что метаданные получить не удалось.
Лимит упёрся раньше, чем прочитаны мастера, — так и скажи в ЧЕГО НЕ ХВАТИЛО
и снизь УВЕРЕННОСТЬ: часть обязательного основания осталась непрочитанной.""",
)

# 7b. Черновик пишется для коллеги, а не для аналитика
sub(
    """ПРАВИЛА ПРО БЛОК «ИСТОЧНИКИ»""",
    """ЧЕРНОВИК ПИШЕТСЯ ДЛЯ КОЛЛЕГИ, А НЕ ДЛЯ АНАЛИТИКА
ЧЕРНОВИК ОТВЕТА уйдёт человеку в чат поддержки. Он не работает с базой знаний,
не вызывает инструментов и не знает, как устроен ты. Поэтому в ЧЕРНОВИКЕ
не встречается ничего служебного:
— имена инструментов: read_kb_article, list_kb_folder, dd_lookup, get_table_info
  и любые другие. Даже если инструмент упомянут в тексте прочитанной статьи —
  в черновик он не переносится;
— пути к статьям базы знаний вида kb/tables/…, kb/recipes/…, их id вида
  t-emp-structure, rc-field-synonyms и слова «база знаний», «реестр», «статья»;
— служебная разметка базы: «домен», «мастер-сущность», названия доменов вида
  headcount, movement, allocation. Это твоя внутренняя навигация,
  коллеге она ничего не говорит;
— названия каталогов метаданных и внутренних систем: Data Detective, DD.
Всё это место в блоке ИСТОЧНИКИ — он для джуна, который проверяет черновик.
В самом черновике остаётся только предметное: имя витрины и схема, имена полей,
фильтры, что нужно уточнить у заказчика.

СРЕДА, В КОТОРОЙ ПИШУТСЯ ЗАПРОСЫ
Аналитика по HR-витринам делается в ноутбуках Helicopter. Ни одной другой среды
и ни одного клиента к БД не называй: ни DBeaver, ни pgAdmin, ни DataGrip,
ни консоль psql. Их в команде не используют, и совет открыть их — прямая
дезинформация. Нужно предложить проверить что-то запросом — это «запрос
в ноутбуке Helicopter».
Инструменты, которые упоминаются в статьях базы знаний, — например get_table_info —
это инструменты аналитика в его среде, а не твои и не инструменты коллеги.
Не предлагай коллеге их вызвать. Состав полей выясняешь ты сам через dd_lookup
и пишешь в черновике результат, а не поручение его добыть.

ПРАВИЛА ПРО БЛОК «ИСТОЧНИКИ»""",
)

# 8. Финальное напоминание
sub(
    """НАПОМИНАНИЕ (важнее всего остального)
Реестр — только для поиска пути. Прежде чем утверждать что-либо, прочитай саму
статью инструментом read_kb_article. Не отвечай по колонке «описание».""",
    """НАПОМИНАНИЕ (важнее всего остального)
Реестр — только для поиска пути и для dd_urn. Прежде чем утверждать что-либо,
прочитай саму статью инструментом read_kb_article. Не отвечай по колонке
«описание». Сначала домен, потом мастера домена, потом остальные статьи:
мастер читается всегда, даже когда кажется, что вопрос узкий и ответ уже есть.
Про поля не утверждай ничего без ответа dd_lookup: имена и типы
полей живут в DD, а не в статье. Имя поля не угадывается: сначала пустой
search и полный инвентарь, потом выбор из него.
ЧЕРНОВИК читает коллега в чате: без имён инструментов, без путей kb/,
без слов «база знаний» и «Data Detective». Запросы пишутся в ноутбуках
Helicopter, никакие другие среды не называются.""",
)

assert p != before
agent["parameters"]["options"]["systemMessage"] = p
json.dump(wf, open(F, "w", encoding="utf-8"), ensure_ascii=False, indent=2)
print("OK, prompt:", len(before), "->", len(p), "chars")
print()
print("ВНИМАНИЕ: это промпт СТАРОГО флоу Support Bot DD.json (tool-loop).")
print("В ядро Support Bot Core.json он не попадает — там prompts/router.md")
print("и prompts/author.md. Правка промпта бота идёт туда, затем:")
print("  python3 build_time_flows.py")
